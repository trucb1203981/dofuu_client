import Cookies from 'js-cookie'
export default function ({store, params, redirect}) {
	// store.dispatch('fetchCity')
    // if(typeof Cookies.get('flag_c') == 'undefined' || Cookies.get('flag_c') === null) {
    //     store.dispatch('getCity', 10001)
    // } else {
    //     // const city = store.getters.getCityBySlug(params.city)
    //     store.dispatch('getCity', city.id)
    // }
}